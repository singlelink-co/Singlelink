query hello {
    hello
}

query listLinks {
    listLinks {
        id
        label
        content
        type
        position
    }
}

query findLinkById($id: Int!) {
    findLinkById(id: $id) {
        id
        label
        content
        type
        position
    }
}