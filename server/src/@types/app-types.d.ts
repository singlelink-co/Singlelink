/**
 * Sorting represents the sort mode for the marketplace.
 */
type AddonSorting = "new" | "featured" | "allTimeInstalls" | "currentInstalls" | "lastUpdated";

/**
 * Types of JWT tokens.
 */
type TokenType = "auth" | "passwordReset" | "google_oauth";
