{
  "name": "singlelink",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "build": "cd editor && npm run build && cd ../api && npm run build && cd ../renderer && npm run build",
    "setup": "node setup.js"
  },
  "dependencies": {
    "bufferutil": "^4.0.3",
    "fs": "0.0.1-security",
    "minio": "^7.0.18",
    "pageres": "^6.2.2",
    "prompt": "^1.1.0"
  },
  "devDependencies": {
    "@types/minio": "^7.0.7"
  }
}
